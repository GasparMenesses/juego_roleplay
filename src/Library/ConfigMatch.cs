namespace Library;

public class ConfigMatch
{
    public void DoEncounter(List<Hero> hero, List<Enemy> enemigo)
    {
        if (hero.Count <= 0 || enemigo.Count <= 0)
            return;
        for (int i = 0; i  < enemigo.Count;i++)
        {
            
        }
        
             
    }
    
}